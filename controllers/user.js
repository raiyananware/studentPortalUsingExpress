const db=require('../static/db.js')

const getUsers= async (req, res)=>{
    // const [rows, fields]= await db.query('select * from auth_user')
    // res.status(200).json(rows)

}

module.exports= {getUsers}